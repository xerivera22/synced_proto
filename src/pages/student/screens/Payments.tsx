import type React from "react";
import { PaymentStatus } from "../components/payment-status";

const Payments: React.FC = () => {
  // Render the detailed Payment Status module
  return <PaymentStatus />;
};

export default Payments;
